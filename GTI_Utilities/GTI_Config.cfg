EventConfig
{
	initEvent = true					//Is the event activated on install
	EventCheckFreqIdle = 250			//ms waiting before next check for event when idle
	EventCheckFreqActive = 90			//ms waiting before next check for event when throttle just changed
}
MISCELLANEOUS	//Default settings -- Can be changed in the menu
{
	ActivateLoadFixer = true							//Load Fixer enabled
	ActivateDAI = true									//Docking Alignment Indicator enabled?
	ActivateDoubleTabForBrakeLock = true				//Activate double tab for brakes
	ActivateCameraFocusChanger = true					//Activate CameraFocusChanger
	ActivateCrowdSourcedScienceFixer = true				//Activate CrowdSourcedScienceFixer
	ActivateProjectManager = true						//Activate Project Manager
}
Debug
{
	DebugActive = false			//Debug logs from GTI plugin enabled?
	DebugLevel = Low			//None, Low, Medium, High, VeryHigh, DebugInfo
}