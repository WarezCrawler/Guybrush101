EventConfig
{
	initEvent = true					//Is the event activated on install
	EventCheckFreqIdle = 500			//ms waiting before next check for event when idle
	EventCheckFreqActive = 100			//ms waiting before next check for event when throttle just changed
}